<div class="playlist-name">Recommended</div>

{{#each items}}
<div class="track-container">
    <div class="track-name">Song: {{this.name}}</div>
    <div class="artist-name">Artist: {{this.artist}}</div>
</div>
{{/each}}